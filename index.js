/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Idle from './src/login/LoginPage';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Idle);
